node_modules/
# When this tree is copied into the archived Pages checkout, never stage embedded git metadata
_embedded_git/

# MiniDapp build artifacts: the published download is dapp/latest-version/, never a build/ subdir
dapp/**/build/*.zip
dapp/build/*.zip
