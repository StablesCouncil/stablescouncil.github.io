# Stables v0.0.11.63 testing prerelease

This is a valueless testing build. Winiwa and xWiniwa are test assets with no monetary value. It
is not a stablecoin launch, trading release or production-readiness claim.

## What is released

- `Stables_v0.0.11.63.apk`: the standalone Android app with an embedded Minima node. Package
  `org.stablescouncil.stables`, versionCode 11063. Install over the previous version. Do not
  uninstall: the wallet lives on the device.
  SHA-256: `311f22116063fd557f017caa16d694b6510644dfbb8cdc6975528cb43ded9f06`
- `StablesCore_v0.0.11.63.apk`: the Minima Core companion. It has no node of its own and no Internet
  permission: it pairs with the official Minima Core Android app on the same phone and uses that
  node and that wallet. Package `org.stablescouncil.stables.core`, versionCode 11063. Requires
  Minima Core (tested with 1.6.11). Test tokens only, no value.
  SHA-256: `e63e6db4047dd9b3dc0e26883a37086ab51b413d925e4fe0bb341f61ca4a91ac`
- Both APKs are signed with the same certificate, SHA-256 `dabb1b2a79b134b6008e6401735d649c140b51f2c4a83eb001b2ffdad5ce5dd4`.

## Coming soon

The MiniDapp package for MinimaOS and the web build follow later. The website shows them as coming
soon; nothing on the site offers them for download yet.

## What is new since v0.0.11.60: the battery release

Everything in this release is about what the app asks its node when nobody is waiting on anything,
measured on a phone before and after.

- The Minima Core companion asks Core about ten times less while idle and nothing at all from the
  pocket, with the same screens and the same freshness: Core's own pushes carry the app between
  reads, a balance or block push that arrives while the app is hidden is remembered and answered on
  return, and an incoming payment still lands through the transaction push within seconds. Measured
  on a Pixel: 8.2 commands a minute on screen to 3.0; 2.25 a minute in the background to 0.33.
- The standalone app no longer re-tracks an old faucet claim on every open. It used to resume the
  most recent faucet claim that was not confirmed, whatever its verdict, and ask for the history
  every five seconds for ten minutes, with a "Faucet claim syncing" card on the Wallet the whole
  time. A claim is resumed only while it is genuinely settling.
- Merged mint and burn rows are no longer re-judged, removed and re-created on every open, and an
  old failed row no longer makes every open re-read transactions that were never this wallet's.
- The on-chain chat scans every 15 seconds only while the Chat page is on screen; elsewhere it is a
  two-minute safety net, kicked at once by the node's transaction push.
- A node that cannot yet prove the faucet pool is retried gently instead of sixty times a minute.
- The app keeps a ledger of who asked the node what and prints it every five minutes on screen, so
  the next cadence is attributed rather than guessed.

Measured on the same Pixel, standalone, idle on the Wallet page: 24 node commands a minute at
v0.0.11.62 to 5 to 9 a minute at v0.0.11.63; the first five minutes after an open: 277 commands to 78.

Full changelog: `dapp/3-test/CHANGELOG.md`, section `[0.0.11.63] - 2026-09-06`.
