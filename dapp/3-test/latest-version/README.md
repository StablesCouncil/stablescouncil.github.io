# Test channel — `latest-version`

When Council publishes a **test** line **`.mds.zip`**, place the current package here (same naming convention as other channels).

**Policy:** `0_handshake/minidapp_version.md`, **`dapp/MINIDAPP_VERSIONING.md`**.
